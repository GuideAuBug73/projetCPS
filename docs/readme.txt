Git:

git branch: liste toutes les branches
git branch "nom": créer une branche nom
git checkout "nom": passe sur la branche nom

Récupérer données: git pull
Pousse données
  - git add --all
  - git commit
  - git push origin "nom" de la branche
